<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Routes" parent="AppTheme.NoActionBar">
    </style>

    <style name="AppTheme.NumberProgressBar" parent="NumberProgressBar_Default">
        <item name="progress_unreached_color">@color/colorPrimary</item>
        <item name="progress_reached_color">@color/colorPrimaryDark</item>
        <item name="progress_text_color">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
